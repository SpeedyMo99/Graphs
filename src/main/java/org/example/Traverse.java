package org.example;

public class Traverse {
/*
    public static void BFS(Graph D) {
        System.out.println("\n===\nBFS\n===");
        // ...
        // BFS finished:
        D.printResults();
    }

    public static void DFSrecurse(Graph D) {
        System.out.println("\n===\nDFS(r)\n===");
        // ...
        // DFS finished:
        D.printResults();
    }

    public static void DFSiterate(Graph D) {
        System.out.println("\n===\nDFS(i)\n===");
        // ...
        // DFS finished:
        D.printResults();
    }

    public static void BFSVisit(Graph D, Node s) {
        NodeOperation.process(s);
        // ...
    }

    public static void DFSVisitR(Graph D, Node u) {
        NodeOperation.process(u);
        // ...
    }

    public static void DFSVisitI(Graph D, Node s) {
        NodeOperation.process(s);
        // ...
    }

    public static void main(String[] args) {
        Graph g = Graph.loadFromEdgeFile(args[0]);

        for (Node v : g.nodes.values()) {
            System.out.println(v.name);
            System.out.println("\tin:");
            for (Edge e : v.in) {
                System.out.println("\tfrom " + e.fromNode.name +
                        " to " + e.toNode.name);
            }
            System.out.println("\tout:");
            for (Edge e : v.out) {
                System.out.println("\tfrom " + e.fromNode.name +
                        " to " + e.toNode.name);
            }
        }
        DFSrecurse(g);
        DFSiterate(g);
        BFS(g);
    }
*/
}
